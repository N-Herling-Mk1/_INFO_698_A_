Drop logo.png in this directory.

The splash page (index.html) and nav page (nav.html) reference:
  assets/logo.png

If the file is missing, both pages show a graceful fallback frame
so the layout still looks intentional during dev.
